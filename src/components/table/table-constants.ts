import { colors } from '../../global.styles'

export const tableStyle = {
  overflow: 'scroll',
  width: '90%',
  height: '550px',
  border: `1px solid ${colors.hover}`,
  boxShadow: '0px 0px 6px 2px rgba(31, 24, 24, 0.2)',
}
